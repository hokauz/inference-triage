# Platform

Language-neutral contracts and SQL database artifacts live here. Runtime code
must not be added to this directory.

- `contracts/` will contain versioned contracts shared by every runtime.
- `database/` will contain SQL migrations, read views, and reference seeds.
